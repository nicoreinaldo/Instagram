//hacemos referencia al imagenes de el index
$('#images').append('<%= j render @images %>')

//si tiene segunda pagina, sino ni lo hacemos
<% if @images.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate @images %>')
<% else %>
	$('.pagination').remove();
<% end %>
